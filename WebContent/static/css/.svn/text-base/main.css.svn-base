/* Override some defaults */
html, body {
  background-color: #eee;
}
body {
  padding-top: 40px; /* 40px to make the container go all the way to the bottom of the topbar */
}
.container > footer p {
  text-align: center; /* center align it with the container */
}

	#selected
	{
		border:2px solid;
	}

/* The white background content wrapper */
.content {
  background-color: #fff;
  padding: 20px;
  margin: 20px -20px 0px -20px; /* negative indent the amount of the padding to maintain the grid system */
  -webkit-border-radius: 6px;
     -moz-border-radius: 6px;
          border-radius: 6px;
  -webkit-box-shadow: 0 1px 2px rgba(0,0,0,.15);
     -moz-box-shadow: 0 1px 2px rgba(0,0,0,.15);
          box-shadow: 0 1px 2px rgba(0,0,0,.15);
}

/* Page header tweaks */
.page-header {
  background-color: #f5f5f5;
  padding: 20px 20px 10px;
  margin: -20px -20px 20px;
}



.topbar .btn {
  border: 0;
}

.edit {
	background-color:whitesmoke;
	border-radius:4px;
	border:1px solid rgba(0,0,0,0.05);
	-webkit-border-radius:4px;
	box-shadow:inset 0 1px 1px rgba(0,0,0,0.05);
}


.row > .offset0andhalf
{
    margin-left:50px;	
}

.span2andhalf
{
	width:130px;
}

.label{
	margin-left:3px;
}


.userList
{
	min-height:150px;
	/* width:200px;*/
}

.draggedUser {
	zoom: 1;
	opacity: .2;
	-moz-opacity: .2;
	filter: alpha(opacity=20)
}

.bucket{
	background:whiteSmoke;
  -webkit-border-radius: 6px;
     -moz-border-radius: 6px;
          border-radius: 6px;
  -webkit-box-shadow: 0 1px 2px rgba(0,0,0,.15);
     -moz-box-shadow: 0 1px 2px rgba(0,0,0,.15);
          box-shadow: 0 1px 2px rgba(0,0,0,.15);
	min-height:300px;
}


.unread{
	margin-left: -20px;
	background-position-x:20px;
	background-repeat-x:10;
	
}

.read{
	background: #EFEFEF; /*read message color in GMail */
	margin-left: -20px;
	background-position-x:20px;
	background-repeat-x:10;
	
}

.topic
{
	text-align:center;
}

.unread .topic, .unread .words
{
font-weight:bold;
}

hr 
{
	margin:0px;
	border-bottom: 1px solid darkgrey;
	width:2px;
}


.selected
{
	border-top: 2px dotted black;
	border-left: 2px dotted black;
	border-right: 2px dotted black;
	margin-left:-2px;.small {width:50px;}
	
	margin-right:-2px;
}

.selected-panel
{
	border-bottom: 2px dotted black;
	border-left: 2px dotted black;
	border-right: 2px dotted black;
	margin-left:-2px;
	margin-right:-2px;
}


/* Autocomplete
----------------------------------*/
.ui-autocomplete { position: absolute; cursor: default; }       
.ui-autocomplete-loading { background: white url('images/ui-anim_basic_16x16.gif') right center no-repeat; }

/* workarounds */
* html .ui-autocomplete { width:1px; } /* without this, the menu expands to 100% in IE6 */

/* Menu
----------------------------------*/
.ui-menu {
        list-style:none;
        padding: 2px;
        margin: 0;
        display:block;
}
.ui-menu .ui-menu {
        margin-top: -3px;
}
.ui-menu .ui-menu-item {
        margin:0;
        padding: 0;
        width: 100%;
}
.ui-menu .ui-menu-item a {
        text-decoration:none;
        display:block;
        padding:.2em .4em;
        line-height:1.5;
        zoom:1;
}
.ui-menu .ui-menu-item a.ui-state-hover,
.ui-menu .ui-menu-item a.ui-state-active {
        margin: -1px;
}

#next-round
{
	margin-top:20px;
	margin-left:800px;
}

#start-test
{
	margin-top:0px;
	margin-left:750px;
}

#search
{
	margin-left:50px;
}

#answer
{
	rows:5cm;
	cols:100cm; 
}

#round
{
	width:60px;
}
#timing
{
	width:100px;
}

.topicword0 {
	font-size:32pt; 
	font-weight:bold;
}

.topicword1 {
	font-size: 28pt;
	font-weight:bold;
}

.topicword2 {
	font-size:26pt;
}

.red {
	color:red;
}

.topicword8 {
	font-size:8pt;
}